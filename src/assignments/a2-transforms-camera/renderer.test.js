import { describe, it } from "vitest";

// TODO: once you implement src/lib/math/transforms.js, its own test file
// (transforms.test.js, alongside it) is the right place for matrix-math
// tests. Use this file for renderer-specific logic you can extract as a
// pure function — e.g. "given orbit yaw/pitch/distance, what's the eye
// position passed to lookAt?".
describe("assignment 2 — transforms & camera", () => {
  it.todo("derives an eye position from orbit yaw/pitch/distance");
});
