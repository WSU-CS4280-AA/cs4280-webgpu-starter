import { describe, it } from "vitest";

// TODO: extract pure logic (e.g. "sample N points along the curve for
// rendering as a line strip") and test it here. See src/lib/math/curves.js
// for the underlying evaluation functions to test directly in their own
// curves.test.js.
describe("assignment 5 — curves & splines", () => {
  it.todo("samples the curve at evenly spaced t values for line-strip rendering");
});
