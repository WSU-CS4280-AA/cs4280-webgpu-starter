import { describe, it } from "vitest";

// TODO: extract any pure lighting math you move into JS (rather than WGSL)
// and test it here — e.g. a CPU-side reference implementation of your
// Blinn-Phong term to sanity-check the shader's output against.
describe("assignment 3 — lighting & anti-aliasing", () => {
  it.todo("computes the expected diffuse term for a known light/normal pair");
});
