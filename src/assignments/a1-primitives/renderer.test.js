import { describe, it } from "vitest";

// TODO: as you build out `renderer.js`, extract any pure logic you can
// (e.g. "given these clicked points and a mode, what vertex array do we
// upload?") into a plain function you can unit test here without a real
// GPUDevice. See src/lib/math/*.test.js for the pattern this repo uses.
describe("assignment 1 — 2D primitives", () => {
  it.todo("builds the correct vertex buffer for point mode");
  it.todo("builds the correct vertex buffer for line mode");
  it.todo("builds the correct vertex buffer for triangle mode");
});
