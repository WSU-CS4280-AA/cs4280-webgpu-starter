import { describe, it } from "vitest";

// TODO: whatever pure logic your feature needs (keyframe interpolation,
// a skinning weight calculation, a BRDF term, ...) is worth extracting and
// testing here — see src/lib/math/*.test.js for the pattern.
describe("assignment 7 — final project", () => {
  it.todo("add tests for whatever advanced feature you build");
});
